#================================================
# tools/__init__.py
#
# tools 를 패키지로 만든다. 안의 스크립트들이 서로를 상대 import 로
# 가리킬 수 있게 하려는 것이다.
#
# 이 파일이 생기면서 실행 방식이 바뀐다.
#   python tools/regression_check.py   ->  ImportError
#   python -m tools.regression_check   ->  동작
# 상대 import 는 부모 패키지를 아는 상태에서만 풀리고, 파일을 직접
# 실행하면 그 파일이 최상위 스크립트가 되어 부모를 모른다.
#================================================
