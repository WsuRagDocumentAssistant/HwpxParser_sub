#================================================
# tools/defaults.py
#
# 도구들이 인자 없이 실행될 때 쓸 기본 문서.
#
# 전에는 이 경로가 스크립트 7곳에 각각 적혀 있었다. 대상 문서를 바꾸려면
# 일곱 군데를 다 고쳐야 했고, 하나라도 빠뜨리면 도구마다 다른 문서를 보게
# 된다. 회귀 기준선은 문서 하나에 묶여 있으므로 그건 조용한 오판정으로
# 이어진다. 그래서 한 곳에서만 정한다.
#
# 다른 문서로 돌리려면 각 도구의 source 인자나 --source 를 쓴다.
#================================================

from __future__ import annotations

from pathlib import Path

REPO_ROOT = Path(__file__).resolve().parent.parent

DEFAULT_SOURCE = REPO_ROOT / (
    "3주기(2025년) 대학혁신지원사업 1차년도 수정 자율혁신계획_260122 1133.hwpx"
)
